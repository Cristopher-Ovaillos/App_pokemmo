const LOCAL_IP = '192.168.1.71';  //insert IP, en la terminal cmd de windows insert ipconfig (copiar la ip de direccion ipv4)

export const API_URL = `http://${LOCAL_IP}:5000`;
export const IMAGE_URL = `${API_URL}/images`;
